import Foundation

/// Copy to Secrets.swift and fill admin key for dev Pro auto-grant.
enum Secrets {
    /// Never store Render `API_KEY`, `service_role`, OpenAI, or webhook secrets in the app.
    static let adminApiKey: String? = nil
}
