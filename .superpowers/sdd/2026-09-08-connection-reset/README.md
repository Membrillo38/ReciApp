# Obsolete SDD report

This folder is historical (connection-reset when the stack still used hosted Auth/DB).

**Current stack:** VPS Coolify + self-hosted Postgres. Do not follow Supabase or Render steps here.
